---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: starkandwayne/concourse

inputs:
- name: pipeline-tasks
- name: mc-release

outputs:
- name: mc-cli

run:
  path: pipeline-tasks/download-mc-cli.sh
